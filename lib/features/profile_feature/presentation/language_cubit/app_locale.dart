import 'package:flutter/material.dart';

class AppLocale {
  static const english = Locale("en");
  static const arabic = Locale("ar");
  static const supportedLocal = [
    english, // English
    arabic, // Arabic
  ];
}
